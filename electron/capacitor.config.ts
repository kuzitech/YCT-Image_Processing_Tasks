import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'imagetask.kuzitech.app',
  appName: 'imagetask',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
